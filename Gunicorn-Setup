step-by-Step Gunicorn Setup 
🔹 Step 1 — Go to your app directory
cd /home/sowtheswari22
ls

You should see:

app.py  flask-env

🔹 Step 2 — Activate virtual environment
source flask-env/bin/activate


You should see:

(flask-env)

🔹 Step 3 — Install Gunicorn (inside venv)
pip install gunicorn


Verify:

gunicorn --version

🔹 Step 4 — Verify your Flask app variable

Open your app:

nano app.py


You must have this line:

flaskapp = Flask(__name__)


👉 The variable name (flaskapp) is important for Gunicorn.

🔹 Step 5 — Test Gunicorn manually (one-time)

Run:

gunicorn app:flaskapp --bind 127.0.0.1:8000


Open browser:

http://SERVER-IP:8000


If it works → Gunicorn is correct ✅
Press CTRL + C to stop.
